# Workflow

1. run `generate_polytopes.jl` to generate instances
2. run `get_solutions.jl` to find a globally optimal solution of the instances generated in the previous phase
3. run `run_fw_variants.jl` to test several FW variants on those instances